// src/environments/environment.production.ts
// ── Production environment ───────────────────────────────────────────────────
export const environment = {
  production: true,
  apiUrl: 'http:///api',  // ← change before deploying
  tokenKey: 'cs_token',
  userKey:  'cs_user',
};